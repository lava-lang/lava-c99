import <stdio.h>;

int main() {
    if (1) {
        f32 x = 6;
    } else {
        int x = 5;
    }

    if (1) {
        f32 x = 6;
    } else if (2) {
        int x = 5;
    }
}

/*
struct Vec {
    int x, y, z;

    int offset(int w) {
        return x + y + z + w;
    }

    int size() {
        return x + y + z;
    }
}

int test() {
    return 1;
}

void fizzBuzz(int max) {
    int i = 0;
    while (i < max) {
        if (i % 3 == 0) {
            c.printf("Fizz");
        }
        if (i % 5 == 0) {
            c.printf("Buzz");
        }
        if ((i % 3 != 0) && (i % 5 != 0)) {
            c.printf("number=%d", i);
        }
        c.printf("\n");
        i++;
    }
}

int main() {
    int z = 5;
    Vec v = {z, test(), 4};
    int aa = v.offset(1);

    bool condition = true;
    if (z % 5) {
        c.printf("the condition is true\n");
    }

    while(!condition) {
        c.printf("infinite loop");
        break;
    }

    c.printf("aa: %d\n", aa);
    c.printf("v.x: %d\n", v.x);

    fizzBuzz(20);
}*/