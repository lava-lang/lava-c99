struct Vec {
    int x;
    int y;
    int z;

    Vec* add(Vec* v) {
        Vec* r = {x + v.x, y + v.y, z + v.z};
        return r;
    }
}

int main() {
    Vec* v1 = {1, 1, 1};
    Vec* v2 = {1, 1, 1};

    Vec* v3;

    Vec* r = v1.add(v2);
}